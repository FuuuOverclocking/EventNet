import { IAttrFuncCondition } from "../../types"

export default {
    priority: 10,
    before: (value: any, condition: IAttrFuncCondition) => {
        //////////////////////////////////////////////
    }
};